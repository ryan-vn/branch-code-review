interface:
  display_name: "Branch Code Review"
  short_description: "Review branch diffs with local impact triage"
  default_prompt: "Use $branch-code-review to review all code developed on the current branch since the branch was created, include working tree changes, and produce a risk-focused report using CodeGraph when available or the no-CodeGraph local impact map when it is not."
